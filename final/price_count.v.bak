module price_count(reset,dis_en,time_en,double,expense,clk)
input clk,reset,dis_en,time_en,double;
output reg [7:0]expense;
always @(posedge clk)begin
	if(!reset)
		expense<=8'd0;
	else begin
		if()
	
	end


end
